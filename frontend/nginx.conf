server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Allow large PDF uploads
    client_max_body_size 200M;

    # SPA routing — return index.html for all unmatched routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
